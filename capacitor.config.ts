import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dealApp',
  appName: 'Deal-Navigation-Menu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
